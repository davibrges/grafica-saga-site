name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ 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 }}
          publish_dir: ./grafica
